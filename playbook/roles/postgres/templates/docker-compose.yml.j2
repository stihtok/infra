version: "3.9"
services:
  postgres:
    image: "postgres:14"
    restart: always
    environment:
      - POSTGRES_PASSWORD={{ postgres_password }}
    volumes:
      - ./data:/var/lib/postgresql/data/